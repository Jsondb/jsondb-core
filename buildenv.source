#
# A file to set environment variables for the developers system.
# Do not execute this file source it using the Linux source command

#Exports
# We expand the relative path so that the Gradle executable is available form anywhere once this files is sourced.
GRADLE_WRAPPER=$(cd $(dirname "./buildsupport/gradlewrapper/"); pwd)/gradlewrapper

export PATH=$PATH:$GRADLE_WRAPPER

#Aliases
alias gw="gradlew"

